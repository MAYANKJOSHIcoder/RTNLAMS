# Copy to scripts/.env (gitignored) and paste the service_role key from
# Supabase Dashboard → Project Settings → API Keys → service_role (NEVER the anon key)
SUPABASE_URL=https://your-project.supabase.co
SERVICE_ROLE_KEY=paste_service_role_key_here
